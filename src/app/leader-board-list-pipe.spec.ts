import { LeaderBoardListPipe } from './leader-board-list-pipe';

describe('LeaderBoardListPipe', () => {
  it('create an instance', () => {
    const pipe = new LeaderBoardListPipe();
    expect(pipe).toBeTruthy();
  });
});
